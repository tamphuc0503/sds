version: '3'

services:  
  rabbitmq:  
    image: rabbitmq
    container_name: 'rabbitmq' # account: guest/guest
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
        - ./data/rabbitmq:/var/lib/rabbitmq/
        - ./data/rabbitmq:/var/log/rabbitmq
  # redis:
  #   image: redis:6.2-alpine
  #   restart: always
  #   ports:
  #     - '6379:6379'
  #   command: redis-server # --save 20 1 --loglevel warning --requirepass eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81    
  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:7.14.0
  #   restart: always
  #   ports:
  #     - '9200:9200'    
  #   environment:
  #     - discovery.type=single-node      
  #     - cluster.name=docker-cluster
  #     - bootstrap.memory_lock=true
  #     - http.cors.enabled=true
  #     - http.cors.allow-origin=*
  #     - http.cors.allow-headers=X-Requested-With,Content-Type,Content-Length,Authorization
  #     - "ES_JAVA_OPTS=-Xms512m -Xmx512m"       
  #   volumes:
  #     - ./data/elasticsearch:/usr/share/elasticsearch/data
  # kibana:
  #   image: docker.elastic.co/kibana/kibana:7.14.0
  #   restart: always
  #   ports:
  #     - "5601:5601"
  #   environment:
  #     ELASTICSEARCH_HOSTS: '["http://elasticsearch:9200"]'
  #   depends_on:
  #     - elasticsearch
  sds-django:
    image: mcr.microsoft.com/devcontainers/python:0-3.11
    # network_mode: service:redis
    command: sleep infinity    
    ports:
      - 8000:8000
    environment:
      # - DB_PORT=5432
      # - DB_HOST=10.110.100.20    
      # - DB_NAME=sds_admin
      # - DB_USER=sds_admin
      # - DB_PASSWORD=asdfghjkl228
      - DB_PORT_SDSMANAGER=5432
      - DB_HOST_SDSMANAGER=10.110.100.20    
      - DB_NAME_SDSMANAGER=sds_admin
      - DB_USER_SDSMANAGER=sds_admin
      - DB_PASSWORD_SDSMANAGER=asdfghjkl228
      - DB_PORT_SDS=5432
      - DB_HOST_SDS=s1.sdsfile.com
      - DB_NAME_SDS=sds
      - DB_USER_SDS=sds
      - DB_PASSWORD_SDS=kZpj1FgsXBi75ed
      - APP_ENV=dev
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672      
      #- CELERY_BROKER_URL=amqp://administrator:g3N3rAtED@queue.sdsfile.com:5672
      - SDS_FILE_STORAGE=/app/storage
      - SDS_ADMIN_URL=https://staging-admin55.sdsmanager.com
      - SDS_DOWNLOADER=s1
      # - HTTP_PROXY=http://ahjkugns1-rotate:0ptzphhal1jm1@p.webshare.io/
      - SDS_REDIS_HOST=redis
      - SDS_REDIS_PORT=6379
      - SDS_ELASTICSEARCH_HOST=elasticsearch
    volumes:
    - ..:/app:cached
    depends_on: 
    - rabbitmq
    # - redis
    # - elasticsearch